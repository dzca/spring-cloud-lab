eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:5001/eureka/
      
server:
  port: 5008
  
spring:
  cloud:
    config:
      profile: dev
      uri: http://localhost:5888/
       
zuul:
  routes:
    auth:
      path: /auth/**
      serviceId: auth